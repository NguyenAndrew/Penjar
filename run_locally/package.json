{
  "name": "run_locally",
  "version": "1.0.0",
  "description": "Runs the project locally",
  "main": "index.js",
  "scripts": {
    "install:all": "npm install && concurrently \"cd ../back_end && npm install\" \"cd ../front_end && npm install\""
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^5.3.0"
  }
}
